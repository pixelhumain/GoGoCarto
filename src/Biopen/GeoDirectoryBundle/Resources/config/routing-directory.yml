biopen_directory:
    path:     /annuaire
    defaults: { _controller: BiopenGeoDirectoryBundle:Directory:render }
    options:
      expose: true

biopen_directory_showElement:
    path:     /annuaire#/fiche/{name}/{id}
    defaults: { _controller: BiopenGeoDirectoryBundle:Directory:render }