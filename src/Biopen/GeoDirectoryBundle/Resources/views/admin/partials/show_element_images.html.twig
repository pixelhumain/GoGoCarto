{% extends "@BiopenAdmin/core_custom/show_choice_base.html.twig" %}

{% block display_value %}
    {% for key, item in value %}
      <img src="{{ item.imageName }}" class="thumbnail"/> 
      {#<img src="{{ asset('uploads/image/elements/' ~ item.imageName) }}" class="thumbnail"/> #}
    {% endfor %}

    <style>
      .thumbnail {     
        width: 100px;
        display: inline-block; 
      }
    </style>
{% endblock %}
